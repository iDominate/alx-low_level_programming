#include <stddef.h>
#include <stdio.h>
/**
 * _strpbrk - similar to strpbrk
 *
 * @s: buffer
 * @accept: string
 *
 * Return: pointer
 */
char *_strpbrk(char *s, char *accept)
{
	unsigned int i, j;
	char *result;

	:
	return (NULL);
}
